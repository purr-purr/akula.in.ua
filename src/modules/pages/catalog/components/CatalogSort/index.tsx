import s from './CatalogSort.module.scss';

const CatalogSort = () => {
	return (
		<aside className={s.container}>
			<button>Clear</button>
			<button>Price</button>
		</aside>
	);
};

export default CatalogSort;
